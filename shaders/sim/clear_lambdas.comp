#version 460
#include "../common/params.glsl"

layout(local_size_x = 256) in;
void main(){
    uint id = gl_GlobalInvocationID.x;
    if (id < sim.numEdges) edges[id].lambda = 0.0;
    if (id < sim.numBends) bends[id].lambda = 0.0;
}